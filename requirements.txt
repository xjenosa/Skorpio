# ── Core API ───────────────────────────────────────────────────────────── #
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13
# Required by FastAPI's File()/Form() — used by /api/jobs/import.
python-multipart>=0.0.9

# ── Auth (Stage 2) ────────────────────────────────────────────────────── #
pyjwt[crypto]>=2.8        # JWT issuance + verification
email-validator>=2.0      # backs Pydantic's EmailStr
resend>=2.0               # 6-digit magic-code emails

# ── Anthropic + HTTP ───────────────────────────────────────────────────── #
anthropic>=0.34
httpx>=0.27

# ── Analytics / ML for site space ──────────────────────────────────────── #
numpy>=1.26
scikit-learn>=1.4

# ── Optional: Snowflake Site Intelligence Layer ────────────────────────── #
snowflake-connector-python>=3.10
